@model List<Category>

@* <div class="list-group">
    <a href="/urunler" class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == null ? "active" : "" ) ">
        Tüm Kategoriler
    </a>

    @foreach (var item in @Model)
    {
        <a 
        asp-controller="Shop" 
        asp-action="List" 
        asp-route-kategori="@item.Url" 
        class="list-group-item list-group-item-action @( ViewBag.SelectedCategory == item.Url ? "active" : "" )">
            @item.Name
        </a>
    }
</div> *@

<nav class="navbar navbar-expand-lg bg-body-tertiary navbar-light px-2 rounded mb-4" style="background-color: #e3f2fd;>
  <div class="container-fluid">
    <a class="navbar-brand @(ViewBag.SelectedCategory == null ? "active" : "" )" href="/kategoriler" >Kategoriler</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent2" aria-controls="navbarSupportedContent2" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent2">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
       @foreach (var item in @Model)
        {
            <li class="nav-item">        
                <a 
                asp-controller="Shop" 
                asp-action="List" 
                asp-route-kategori="@item.Url" 
                class="nav-link @( ViewBag.SelectedCategory == item.Url ? "active" : "" )">
                    @item.Name
                </a>
            </li>
        }
      </ul>

      @await Html.PartialAsync("_search")

    </div>
  </div>
</nav>

